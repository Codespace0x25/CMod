#include "../../include/cmod.h"
#include <stdio.h>
#include <stdlib.h>


int main(int args,char *argv[]){
  if (args LESS_THAN_OR_EQUAL 0){
    fprintf(stderr,"error; you must pass a file of  .c or .cm");
    return 1;
  }

  FILE *filed = fopen(argv[1],"r");
  String* data;

  //read data from files and puting it in data using  String *string_create(const char *initial_data);


  pprintf("%s",data);// custom printf dont make or touch



  return 0;
}
